export const ChosenElement = new Set();

export const IdRow = new Set();

export const IdEvents = new Set();

export var DataDate = {
    startDate: undefined,
    endDate: undefined
}

export var placeEvent = {
    place: undefined
};